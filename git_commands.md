# Step-01 - Initialize a git repo locally

Git init

# Step-02 - Initiate tracking on Git

Git add . (for all files)
git add <filename> (for specific file)

# Step-03 - Stage the files

git commit -m "<commit description>"
